version: '3.8'

services:
  web:
    build: .
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
    # This section allows the container to see your host machine
    extra_hosts:
      - "host.docker.internal:host-gateway"